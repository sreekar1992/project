apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ecg-workbench-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  # Set storageClassName for your cluster when its default class is unsuitable.
